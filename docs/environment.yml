name: gcpy
channels:
    - defaults
    - conda-forge
dependencies:
    - python=3.6       # Python version 3.6
    - bottleneck       # C-optimized array functions for NumPy
    - cartopy=0.17.0   # Geographic plotting toolkit
    - cython           # Transpile Python->C
    - dask             # Parallel processing library
    - esmpy            # ESMF for python
    - graphviz         # visualize dask graph (binary)
    - future           # Python 2/3 compatibility
    - h5py             # Wrapper for HDF5
    - ipython          # IPython interpreter and tools
    - jupyter          # Jupyter federation architecture
    - matplotlib=3.1.1 # 2D plotting library
    - netcdf4          # Wrapper for netcdf4
    - notebook         # Notebook interface
    - numpy=1.19.1     # N-d array and numerics
    - pandas=0.25.1    # Labeled array library
    - pip              # Package installer for Python
    - pyresample       # Geographic resampling tools
    - pytest           # Unit/mock testing tools
    - scipy=1.3.1      # Common math/stats/science functions
    - scikit-learn     # Machine learning library
    - statsmodels      # Regression/modeling toolkit
    - seaborn=0.9.0    # Statistical visualizations
    - six              # Python 2/3 compatibility
    - tqdm             # Nice progressbar for longer computations
    - xarray=0.11.3    # N-d labeled array library. Use older version for compatibility with xbpch
    - sphinx
    - pip:
        # These are additional libraries to search for on PyPI
        - xbpch=0.3.5  # Interface for bpch output files
        - h5pyd        # HDF5 for Amazon S3
        - h5netcdf     # allow HDF5-backend for xarray
        - graphviz     # visualize dask graph (python package)
        - joblib=0.15.1# Parallel processing for Python
        - pycodestyle  # tool to check style conventions (formerly pep8)
        - xesmf=0.2.0  # Universal regridder
        - PyPDF2=1.26.0# PDF utilities, add bookmarks to PDFs etc.
        - tabulate     # Pretty-print tabular data in Python